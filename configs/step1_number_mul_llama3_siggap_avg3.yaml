# T2 — number_mul family cell (Llama-3; ported historical matrix,
# final ep49 sha 96ae3b8b5ebc…, artifacts/PORTED_MATRICES.md).
# Split discipline: the committed number_mul_paper.yaml split's TRAIN
# tasks were used to train this (ported) matrix and are the only tasks seen
# by significance/scan/selection; the 5 held-out tasks produce every
# reported accuracy (100/task, materialized only by evaluate-headset).
# FULL leg (headline per Q8): scan 100/task; calib PASSED 2026-07-27 (job
# 1h04m: 37 gap sig heads; unified main (13,6)+4 weak heads,
# localized; held-out clean 0.348 / raw 0.192; dead tasks mul19/mul23
# visible per-task as documented).
# Validity overlay (Q7): held-out mul19/mul23 are dead tasks (clean
# <=0.08 for all models historically) — reported, not excluded.
schema_version: 1
run_name: mulllama_siggap_avg3
protocol: corrected_holdout
model:
  name: meta-llama/Meta-Llama-3-8B-Instruct
  revision: 8afb486c1db24fe5011ec46dfbe5b5dccdb575c2
  dtype: bfloat16
  device: cuda
task:
  dataset_dir: number_mul
  prompt_format: arrow
  n_shot: 5
  task_split: configs/task_splits/number_mul_paper.yaml
sites:
  inject_layer: blocks.10.hook_resid_mid
matrix:
  mode: reuse
  reuse_path: artifacts/matrix_mul_meta-llama-3-8b-instruct_lambda0.05
  node_name: mulllama-avg3
  checkpoint_select: {rule: mean_last_k, k: 3}
samples:
  activation: {examples_per_task: 100, seed: 42, task_set: train}
  scan: {examples_per_task: 100, seed: 42, task_set: train}
  heldout_activation: {examples_per_task: 100, seed: 1043, task_set: eval}
  final_eval: {examples_per_task: 100, seed: 42, task_set: eval}
significant:
  method: largest_gap
scan:
  c_min: 0
  c_max: 20
main_selector:
  name: unified
  version: 1
  params: {beta: 0.25, floor: 0.12, clean_min: 0.25, minor_z: 3.5}
compute:
  wandb_mode: offline
